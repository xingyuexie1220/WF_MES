<UserControl x:Class="WF.MES.WPF.Modules.Equipment.Views.EquipmentTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="24">
        <StackPanel>
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding PageTitle}" />
            <TextBlock Margin="0,8,0,16" Foreground="#666" Text="{Binding HintText}" />
            <StackPanel Orientation="Horizontal">
                <TextBox Width="240" Margin="0,0,8,0" Text="{Binding SerialNo, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="{Binding SubmitText}" Command="{Binding SubmitCommand}" />
            </StackPanel>
            <TextBlock Margin="0,16,0,0" Text="{Binding ResultMessage}" />
        </StackPanel>
    </Grid>
</UserControl>
